# Network name matching deployments/ folder (localhost, hardhat, sepolia)
NETWORK=localhost

# RPC URL for the chain (for API to read/write contracts)
RPC_URL=http://127.0.0.1:8545

# ---------- Account keys (update these for admin / NGO / donor when using API for writes) ----------
# Admin: verify NGO, reject NGO, transfer admin. Use Account #0 key if that's your deployer/admin.
PRIVATE_KEY=

# Optional: NGO wallet key – for API to create/deactivate campaigns as this NGO.
# NGO_PRIVATE_KEY=

# Optional: Donor wallet key – for API to record donations as this donor.
# DONOR_PRIVATE_KEY=

# API server port
PORT=3001

# Sepolia (when using deploy:sepolia)
# RPC_URL_SEPOLIA=https://rpc.sepolia.org
# PRIVATE_KEY=your_hex_private_key
